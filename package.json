{
  "name": "kafka-node-app",
  "version": "1.0.0",
  "description": "Kafka Node.js application",
  "main": "index.js",
  "scripts": {
    "start": "docker-compose up zookeeper kafka",
    "write": "docker-compose run --rm writer",
    "read": "docker-compose run --rm reader",
    "build": "docker-compose build",
    "server": "docker-compose up middleware"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http": "^0.0.1-security",
    "kafkajs": "^2.2.4",
    "readline": "^1.3.0",
    "socket.io": "^4.8.0",
    "uuid": "^10.0.0"
  }
}
